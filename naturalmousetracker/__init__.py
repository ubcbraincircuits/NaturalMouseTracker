# import automate
# import detect_mice
# import crop_videos
# import head_tail_label
# import classify_behaviours
# import readEncoding
#
# def detect_mice(dataDrive, dataPath, showVid=False, frames=False):
#     detect_mice.run(dataDrive, dataPath, showVid, frames)
#
# def crop_videos(dataDrive, dataPath, frames=False):
#     crop_videos.run(dataDrive, dataPath, frames)
#
# def batch_classify(dir, dataDrive, fullPath):
#     automate.main(dir, dataDrive, fullPath)
#
# def head_tail_label(dataDrive, dataPath):
#     head_tail_label.run(dataDrive, dataPath)
#
# def classify_behaviours(dataDrive, dataPath, user, host, db):
#     password = getpass.getpass(prompt="Please enter the password for the database")
#     classify_behaviours.run(dataDrive, dataPath, user, host, db, password)
